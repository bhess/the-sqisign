# Valgrind suppressions for sqisign_test_ct_lattice_<variant> (src/quaternion/ref/lvlx/lll/lll_ct_ctgrind.c).
#
# The reduction kernels themselves report nothing: stage A (quat_lll_reduce_O0_ideal), stage B
# (quat_ideal_reduce_basis) and stage C1 (quat_lll_dual_reduce_ideal) are all clean with zero suppressions.
# The one accepted exemption is below.

# ---------------------------------------------------------------------------
# ACCEPTED EXEMPTION: quat_lattice_bound_parallelogram
{
   ct-lattice: ACCEPTED - non-CT sampling-box computation in quat_lattice_bound_parallelogram
   Memcheck:Cond
   ...
   fun:sqisign_gen_ibz_*
   fun:sqisign_gen_quat_lattice_bound_parallelogram
}
{
   ct-lattice: ACCEPTED - non-CT sampling-box computation in quat_lattice_bound_parallelogram (value uses)
   Memcheck:Value8
   ...
   fun:sqisign_gen_ibz_*
   fun:sqisign_gen_quat_lattice_bound_parallelogram
}
{
   ct-lattice: ACCEPTED - inlined ibz_set_bound -> normalise in quat_lattice_bound_parallelogram
   Memcheck:Cond
   fun:normalise
   fun:sqisign_gen_quat_lattice_bound_parallelogram
}
{
   ct-lattice: ACCEPTED - inlined ibz_set_bound -> normalise in quat_lattice_bound_parallelogram (value uses)
   Memcheck:Value8
   fun:normalise
   fun:sqisign_gen_quat_lattice_bound_parallelogram
}
{
   ct-lattice: ACCEPTED - propagate_bit under the same inlined normalise
   Memcheck:Value8
   fun:propagate_bit
   fun:normalise
   fun:sqisign_gen_quat_lattice_bound_parallelogram
}
{
   ct-lattice: ACCEPTED - mp_shl_words under ibz_div, unwind fails past this frame
   Memcheck:Value8
   fun:mp_shl_words
}
{
   ct-lattice: lazy PLT binding (register save/restore false positive)
   Memcheck:Value8
   fun:_dl_runtime_resolve
}
